// Template literals are an easier way to create strings.
const name = "John"
const msg = `Hello, ${name}`;
console.log(msg);

// Multi-line strings
const email = `
Hello,

This is a multi-line message.

Regards,
JS
`;
console.log(email);